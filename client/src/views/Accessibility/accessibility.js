import React, { Component } from 'react';

class accessibility extends Component{
    render() {
        return (
            <div>
              <button type='button' onClick={this.DarkMode}>Enable DarkMode</button>
            </div>
        )
    }

}

export default accessibility